# Optional OCR engines for benchmarking.
# Install only the providers you plan to test; some packages are large.

pytesseract
easyocr
transformers
# For local TrOCR on NVIDIA GPUs, install the CUDA PyTorch build from the official
# PyTorch index instead of the default CPU-only wheel when needed.
torch
torchvision
accelerate
timm
